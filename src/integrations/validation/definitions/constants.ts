
const FieldTypes = {
    STRING: 'string',
    NUMBER: 'number',
    BOOLEAN: 'boolean',
    DATE: 'date',
    UUID: 'uuid',
    EMAIL: 'email',
    ARRAY: 'array'
};

Object.freeze(FieldTypes);

export { FieldTypes };
